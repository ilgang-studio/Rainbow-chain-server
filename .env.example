# Socket.IO JWT authentication secret.
# Must be set in production (e.g. Render environment variables).
# Generate a strong random value: openssl rand -hex 32
SOCKET_JWT_SECRET=change-me-to-a-long-random-secret
